# 🧪 로컬 프로덕션 테스트 가이드

## 🚀 빠른 시작

### 1. 배포 실행
```bash
docker-compose -f docker-compose.prod.yml up -d
```

### 2. 접속 주소

배포가 완료되면 다음 주소로 접속하세요:

#### 🌐 Frontend (프론트엔드)
```
http://localhost
또는
http://localhost:80
```

#### 🔧 Backend API (백엔드 API)
```
http://localhost:8000
```

#### 📚 API 문서 (Swagger)
```
http://localhost:8000/docs
```

#### 📖 API 문서 (ReDoc)
```
http://localhost:8000/redoc
```

#### ❤️ 헬스 체크
```
http://localhost:8000/health
```

## 📋 접속 정보 요약

| 서비스 | 주소 | 설명 |
|--------|------|------|
| Frontend | http://localhost | 메인 웹 애플리케이션 |
| Backend API | http://localhost:8000 | REST API 엔드포인트 |
| API Docs | http://localhost:8000/docs | Swagger UI 문서 |
| Health Check | http://localhost:8000/health | 서비스 상태 확인 |

## ✅ 확인 방법

### 브라우저에서 확인
1. **Frontend**: http://localhost 접속
   - 로그인 페이지가 보이면 정상
   
2. **Backend API**: http://localhost:8000/docs 접속
   - Swagger UI가 보이면 정상

### 터미널에서 확인
```bash
# 헬스 체크
curl http://localhost:8000/health

# Frontend 확인
curl http://localhost:80

# 컨테이너 상태 확인
docker-compose -f docker-compose.prod.yml ps
```

## 🔧 문제 해결

### 포트가 이미 사용 중인 경우

**Windows:**
```powershell
# 포트 사용 확인
netstat -ano | findstr :80
netstat -ano | findstr :8000

# 프로세스 종료 (PID 확인 후)
taskkill /PID <PID> /F
```

**Linux/Mac:**
```bash
# 포트 사용 확인
sudo lsof -i :80
sudo lsof -i :8000

# 프로세스 종료
sudo kill -9 <PID>
```

### 다른 포트 사용하기

`.env` 파일을 수정하거나 `docker-compose.prod.yml`에서 포트를 변경:

```yaml
ports:
  - "8080:80"      # Frontend를 8080으로
  - "8001:8000"    # Backend를 8001로
```

그러면:
- Frontend: http://localhost:8080
- Backend: http://localhost:8001

## 🛑 서비스 중지

```bash
# 서비스 중지
docker-compose -f docker-compose.prod.yml down

# 볼륨까지 삭제 (데이터 삭제됨)
docker-compose -f docker-compose.prod.yml down -v
```

## 📝 로그 확인

```bash
# 모든 서비스 로그
docker-compose -f docker-compose.prod.yml logs -f

# Backend만
docker-compose -f docker-compose.prod.yml logs -f backend

# Frontend만
docker-compose -f docker-compose.prod.yml logs -f frontend
```

## 💡 팁

1. **첫 실행**: 이미지 빌드에 시간이 걸릴 수 있습니다 (5-10분)
2. **재시작**: 설정 변경 후 재시작하려면 `docker-compose -f docker-compose.prod.yml restart`
3. **재빌드**: 코드 변경 후 `docker-compose -f docker-compose.prod.yml build --no-cache`

