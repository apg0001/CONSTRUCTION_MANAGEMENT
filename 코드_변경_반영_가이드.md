# 🔄 코드 변경 반영 가이드

## 현재 상황

**`docker-compose.prod.yml`**에서는 소스 코드가 볼륨으로 마운트되어 있지 않습니다.
따라서 **코드 변경 시 재빌드가 필요**합니다.

## 📋 코드 변경 후 적용 방법

### 방법 1: 재빌드 후 재시작 (권장)

```bash
# 변경된 서비스만 재빌드
docker-compose -f docker-compose.prod.yml build backend    # Backend만
docker-compose -f docker-compose.prod.yml build frontend   # Frontend만
docker-compose -f docker-compose.prod.yml build            # 둘 다

# 재시작
docker-compose -f docker-compose.prod.yml up -d
```

### 방법 2: 한 번에 처리

```bash
# 재빌드 + 재시작 (한 번에)
docker-compose -f docker-compose.prod.yml up -d --build
```

### 방법 3: 완전 재생성 (캐시 없이)

```bash
# 캐시 없이 재빌드 (느리지만 확실함)
docker-compose -f docker-compose.prod.yml build --no-cache
docker-compose -f docker-compose.prod.yml up -d
```

## ⚠️ 재시작만으로는 안 됩니다!

```bash
# ❌ 이렇게 하면 변경사항이 반영되지 않습니다
docker-compose -f docker-compose.prod.yml restart
```

**이유**: Docker 이미지 안에 코드가 포함되어 있어서, 코드를 변경해도 이미지를 다시 빌드하지 않으면 변경사항이 반영되지 않습니다.

## 🚀 빠른 개발을 위한 방법

### 개발 환경 사용 (Hot Reload)

개발 중이라면 소스 코드를 볼륨으로 마운트하는 개발용 설정을 사용하세요:

```bash
# 개발 환경 (docker-compose.yml 사용)
docker-compose up -d
```

하지만 현재 `docker-compose.yml`도 볼륨 마운트가 없으므로, 개발용 설정을 추가로 만들어야 합니다.

### 개발용 Docker Compose 생성

빠른 개발을 위해 `docker-compose.dev.yml` 파일을 만들 수 있습니다:

```yaml
services:
  backend:
    volumes:
      - ./backend:/app  # 소스 코드 마운트
    environment:
      - ENVIRONMENT=development
    # 개발 모드로 실행 (자동 재시작)
    
  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_modules  # node_modules는 컨테이너 내부 것 사용
    # 개발 서버 모드
```

## 📝 요약

| 작업 | 명령어 | 설명 |
|------|--------|------|
| **코드 변경 후** | `docker-compose -f docker-compose.prod.yml up -d --build` | 재빌드 + 재시작 |
| **Backend만 변경** | `docker-compose -f docker-compose.prod.yml build backend && docker-compose -f docker-compose.prod.yml up -d backend` | Backend만 재빌드 |
| **Frontend만 변경** | `docker-compose -f docker-compose.prod.yml build frontend && docker-compose -f docker-compose.prod.yml up -d frontend` | Frontend만 재빌드 |
| **재시작만** | `docker-compose -f docker-compose.prod.yml restart` | ❌ 코드 변경 반영 안 됨 |

## 💡 팁

1. **빠른 테스트**: 개발 중에는 로컬에서 직접 실행하는 것이 더 빠를 수 있습니다
2. **프로덕션**: 배포 전에 반드시 재빌드하세요
3. **로그 확인**: 변경사항이 제대로 반영되었는지 로그로 확인
   ```bash
   docker-compose -f docker-compose.prod.yml logs -f backend
   ```

